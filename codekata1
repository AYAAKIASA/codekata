function solution(n) {
    for (let x = 2; x < n; x++) {
        if (n % x === 1) {
            return x;
        }
    }
    return n - 1; // If no such x is found, return n - 1
}

// Test cases
console.log(solution(10)); // Output: 3
console.log(solution(12)); // Output: 11
